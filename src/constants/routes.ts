export const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  REGISTER: '/register',
  DASHBOARD: '/dashboard',
  CUSTOMERS: '/customers',
  REPORTS: '/reports',
  SETTINGS: '/settings',
  SAMPLES: '/samples',
} as const;

export const AUTH_ROUTES = {
  LOGIN: '/login',
  LOGOUT: '/',
} as const;
