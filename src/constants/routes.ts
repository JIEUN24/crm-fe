export const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  DASHBOARD: '/dashboard',
  CUSTOMERS: '/customers',
  REPORTS: '/reports',
  SETTINGS: '/settings',
  SAMPLES: '/samples',
} as const;

export const AUTH_ROUTES = {
  LOGIN: '/login',
  LOGOUT: '/',
} as const;
